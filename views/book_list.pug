extends layout

block content
  h1= title

  ul
    if book_list
      each book in book_list
        li
          a(href=book.url) #{book.title}
          |,  (#{book.author.name})
    else
      li There are no books.
